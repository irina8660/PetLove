import s from "./UserCard.module.css";
// import UserBlock from "../UserBlock/UserBlock";

const UserCard = (user) => {
  return <div className={s.card}>{/* <UserBlock user={user} /> */}</div>;
};

export default UserCard;
